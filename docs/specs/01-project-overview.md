# 01. 프로젝트 개요

> **대상 독자**: 전체 팀 (PM, 디자이너, FE 개발자, AI 개발자)

## TL;DR (핵심 요약)

- **무엇을**: 디자인 시스템을 **실행·조합·코드 복사**까지 한 번에 하는 플랫폼
- **왜**: AI 시대에 "DS로 AI가 일관된 UI를 만들 수 있는가"를 **실행으로 검증**
- **핵심 흐름**: `Storybook → ds.json → Runtime Hub → Copy for AI → IDE`
- **차별점**: 기존 DS 기반 + 실행 중심 + AI 조합 + 바로 코드로

---

## 프로젝트 비전

### 한 줄 정의

> 디자인 시스템을 실행·조합·코드 복사까지 한 번에 하는 플랫폼

### 왜 만드는가?

**AI 시대의 변화**:
- 디자인 시스템 **만드는 비용**은 급격히 낮아짐
- 새로운 기준: "이 DS로 AI가 얼마나 일관된 UI를 만들 수 있는가"

**현재의 문제**:
```
Figma 링크, Storybook 링크, 문서 설명으로는
"AI가 잘 쓸 수 있는지" 검증 불가
```

**우리의 해결책**:
```
디자인 시스템을 AI와 사람이 함께 "실행으로 검증"하는 플랫폼
```

---

## 핵심 흐름

```
┌──────────┐     ┌──────────┐     ┌──────────────┐     ┌──────────────┐
│ Storybook │ ──→ │ ds.json  │ ──→ │ Runtime Hub  │ ──→ │ Copy for AI  │
│ (원본 DS) │     │ (프로토콜)│     │ (실행/조합)  │     │ (IDE로 전달) │
└──────────┘     └──────────┘     └──────────────┘     └──────────────┘
```

| 구성 요소 | 역할 |
|-----------|------|
| **Storybook** | 디자인 시스템의 실행 원본 (만드는 쪽) |
| **ds.json** | 공용 스펙으로 변환한 결과 (전달용 프로토콜) |
| **Runtime Hub** | 실행·조합·AI 채팅·코드 복사 제공 (쓰는 쪽) |

---

## 핵심 화면 구성

```
┌─────────────────────────────────────────────────────────────────┐
│  Header: [로고]  [URL 입력]  [Upload JSON]                      │
├───────────────────────────┬─────────────────────────────────────┤
│                           │                                     │
│  왼쪽 패널                │  오른쪽 패널                         │
│  ┌─────────────────────┐  │  ┌─────────────────────────────────┐│
│  │ 💬 Chat Area        │  │  │                                 ││
│  │ (가이드형 AI 채팅)   │  │  │  Storybook 기반 실행 화면       ││
│  │                     │  │  │  또는                           ││
│  └─────────────────────┘  │  │  Composition Preview            ││
│  ┌─────────────────────┐  │  │                                 ││
│  │ 📦 Component List   │  │  └─────────────────────────────────┘│
│  │ (접히는 목록)        │  │                                     │
│  └─────────────────────┘  │                                     │
│  ┌─────────────────────┐  │                                     │
│  │ ⚡ Actions          │  │                                     │
│  │ [Copy for AI]       │  │                                     │
│  │ [Copy Tokens]       │  │                                     │
│  │ [Export JSON]       │  │                                     │
│  └─────────────────────┘  │                                     │
└───────────────────────────┴─────────────────────────────────────┘
```

---

## 두 가지 사용 방식

| 구분 | Storybook URL 바로 쓰기 | npx ds-hub extract |
|------|-------------------------|---------------------|
| **언제** | 처음 써볼 때 / 데모 | 내 DS를 계속 쓸 때 |
| **설치** | ❌ 없음 | ⭕ 필요 |
| **입력** | Storybook URL | 로컬 프로젝트 |
| **AI 조합 안정성** | 보통 | 높음 |
| **대상** | 남의 DS | 내 DS |

---

## 워크플로우 3가지

### 1. 그냥 써보는 경우 (30초 와우)

**대상**: 백엔드 개발자, 기획자, 처음 온 사람

```
1. Storybook URL 붙여넣기
   ↓
2. 바로 실행 화면 확인
   ↓
3. 채팅으로 페이지 만들어보기
   ↓
4. "쓸 만한지" 판단
```

### 2. 내 DS 제대로 쓰기

**대상**: 디자이너 + 프론트 개발자

```
1. npx ds-hub extract 실행
   ↓
2. ds.json 생성
   ↓
3. 플랫폼에 업로드
   ↓
4. 내 DS 전용 실행 페이지 생성
```

### 3. 개발자 실제 사용 (Vibe Coding)

**대상**: 백엔드 개발자, 혼자 만드는 사람

```
1. 공개된 DS 실행
   ↓
2. 채팅으로 페이지 조합
   ↓
3. 상태(md, variant 등) 직접 조정
   ↓
4. [ Copy for AI ] 버튼 클릭
   ↓
5. IDE에 붙여넣고 vibe coding
```

---

## 채팅 UI 역할

### ⭕ 해야 하는 것

- ds.json을 읽어서 선택지를 안내하는 **가이드형 채팅**
- "디자인 시스템을 걸어다니게 만드는 **네비게이터**"

### ❌ 하면 안 되는 것

- Lovable식 **자유 생성** 채팅
- AI가 **추측**하거나 **임의 조합**

### 단계별 역할

| 단계 | 채팅의 역할 | 예시 |
|------|-------------|------|
| 1막 | 안내자 / 탐색 도우미 | "어떤 컴포넌트가 있나요?" |
| 2막 | 편집 보조자 | "버튼 색상을 primary로 바꿔줘" |
| 3막 | 조합 제안자 | "대시보드 레이아웃 추천해줘" |
| 4막 | 온보딩 가이드 | "이 DS 처음인데 뭐부터 볼까요?" |
| 5막 | 확장 탐색자 | "비슷한 컴포넌트 더 보여줘" |

---

## Copy for AI 출력 형태

```
We use Acme Design System.

TOKENS (use these exact values)
- Primary color: #0052cc
- Spacing: 4/8/16/24 px
- Radius: 4/6/10 px
- Font: Inter

CONFIRMED COMPOSITION (already reviewed)
- FilterBar (sticky: true)
- MetricCard x3
- LineChart (variant: primary)
- DataTable (dense: true)
- Pagination

Generate a React page using existing DS components.
Use the tokens above for spacing and colors.
```

**핵심**: "추측 없이 바로 구현 가능"

---

## 플랫폼 포지션

### ❌ 아닌 것

- 디자인 시스템 마켓
- 컴포넌트 스토어
- UI 라이브러리 갤러리
- Dribbble / Behance 같은 보여주기용

### ⭕ 맞는 것

- **"Run this Design System"** - 실행 기준 공유
- 디자인 시스템을 코드 자산이 아닌 **실행 가능한 레퍼런스**로 만드는 곳

### 공유의 본질

| 기존 방식 | DS-Runtime Hub |
|----------|----------------|
| 다운로드용 공유 | 실행 기준 공유 |
| "이 DS 좋아요" | "우리 상황에 맞는지 빠르게 판단" |

---

## 경쟁 환경 분석

| 서비스 | 하는 것 | 못하는 것 |
|--------|---------|-----------|
| **Storybook/Chromatic** | 컴포넌트 실행, 상태 확인 | 페이지 조합, AI 생성, 바로 가져다 쓰기 |
| **Figma + Dev Mode** | 디자인 → 코드 힌트 | 실제 실행, Storybook 연동, AI 조합 |
| **v0/Lovable/Uizard** | 텍스트 → UI 생성 | **기존 DS 기반 생성**, 팀 규칙 보존 |
| **Backlight/Zeroheight** | DS 문서화 | 실행 중심 UX, AI 조합, vibe coding 연계 |

### DS-Runtime Hub의 차별점

> Storybook + JSON + 실행 + AI 조합 + 바로 코드로 가져가기
> 이 흐름을 **하나의 런타임**으로 묶은 서비스는 아직 없다.

---

## 성공 지표 (KPI)

### 제품 지표

| 지표 | 설명 | 목표 |
|------|------|------|
| 파싱 성공률 | Storybook URL → ds.json 변환 성공 | > 90% |
| 30초 와우 달성률 | URL 입력 후 30초 내 실행 화면 확인 | > 95% |
| Copy for AI 사용률 | 조합 후 복사 버튼 클릭 비율 | > 70% |

### 사용자 경험 지표

| 지표 | 설명 | 목표 |
|------|------|------|
| 첫 조합 시간 | URL 입력 → 첫 Composition 완성 | < 3분 |
| 채팅 정확도 | 가이드형 응답이 ds.json 내용과 일치 | 100% |

---

## 용어 정의

| 용어 | 설명 |
|------|------|
| **ds.json** | 디자인 시스템을 표준 형식으로 변환한 JSON 파일 |
| **Composition** | 여러 컴포넌트를 조합하여 만든 페이지/레이아웃 |
| **Copy for AI** | 조합 결과를 AI 프롬프트 형태로 클립보드에 복사 |
| **가이드형 채팅** | ds.json 내용만 참조하여 안내하는 AI 채팅 (생성 금지) |
| **Vibe Coding** | Copy for AI 결과를 IDE에 붙여넣고 AI와 함께 개발 |
| **토큰** | 색상, 간격, 폰트 등 디자인 시스템의 기본 값들 |

---

## 다음 문서

- [02. 시스템 아키텍처](./02-architecture.md) - 전체 시스템 구조와 데이터 흐름
- [05. 개발 워크플로우](./05-development-workflow.md) - MVP 4단계 개발 계획
