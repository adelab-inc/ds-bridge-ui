# diff(search/replace) 부분 코드 편집 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대형 파일 텍스트 수정 시 모델이 전체 파일 대신 search/replace 패치(`<edit>`)만 출력하고, 서버가 현재 파일에 적용해 전체 파일을 만들어 저장한다(FE 무변경).

**Architecture:** ① 순수 패치 모듈 `code_patch.py`(파싱·적용·퍼지매칭) ② diff 판정·프롬프트 분기(config/components/chat) ③ `StreamingParser` diff 모드(패치를 broadcast하지 않고 누적) ④ `_run_broadcast_generation` diff 분기(패치 적용 → 실패 시 전체출력 폴백). 마스터 스위치 `gemini_diff_edit_enabled` 기본 off.

**Tech Stack:** Python 3.12, FastAPI, pytest (`asyncio_mode=auto`), uv. BE only(`apps/ai-service`). 명령은 모두 `apps/ai-service`에서 `uv run ...`.

**Spec:** [docs/superpowers/specs/2026-06-22-diff-edit-design.md](../specs/2026-06-22-diff-edit-design.md)

**전제(코드 현실 — 검증됨):**
- `build_conversation_history`(chat.py:309)는 `list[Message]`만 반환, 내부에서 base_code 조회(334-362), 수정 시 사용자 메시지에 "전체 코드를 빠짐없이 출력"(376) 주입.
- `_run_broadcast_generation`(chat.py:1117)은 `base_code` 미수신. no-code 재시도 루프(1221-1307), `_extract_fenced_code` 펜스 폴백(1296), `_fire_and_forget_save` 중간저장(1278), 파서 fallback·derail 가드 보유.
- `StreamingParser.__init__`(chat.py:644)은 무인자, `<file>`만 인식.
- `resolve_system_prompt`(chat.py:244) → `generate_system_prompt`(components.py:2406, `RESPONSE_FORMAT_INSTRUCTIONS` 2465 부착). vision은 `get_vision_system_prompt`(2509), figma는 `run_figma_tool_calling_loop` 별도 경로.
- `build_conversation_history` 호출부 2곳: chat.py:826(non-stream `/chat`), chat.py:1030(stream). 둘 다 `from_message_id` 전달.
- config gemini 설정: `gemini_thinking_level`/`gemini_max_output_tokens=65536`/`gemini_nocode_max_retries=1`.

**커밋 규칙(사용자 선호 — skill 기본 override):** 커밋 prefix 대문자(`Feat:`/`Test:`/`Refactor:`), Co-Authored-By 라인 **제외**, 푸시는 명시 요청 시에만. 브랜치: `feat/diff-edit`.

---

## Chunk 1: code_patch.py (순수 패치 모듈)

의존성 0, 완전 단위 테스트 가능. diff 기능의 심장부. TDD로 먼저 만든다.

**Files:**
- Create: `apps/ai-service/app/services/code_patch.py`
- Test: `apps/ai-service/tests/test_code_patch.py`

### Task 1.1: PatchError + parse_edits

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_code_patch.py`

```python
"""diff 패치 파싱/적용 단위 테스트 (순수 함수, 외부 의존 없음)."""
import pytest

from app.services.code_patch import PatchError, apply_edits, parse_edits

EDIT = """\
<edit path="src/A.tsx">
<<<<<<< SEARCH
const x = 1;
=======
const x = 2;
>>>>>>> REPLACE
</edit>"""


def test_parse_single_block():
    edits = parse_edits("설명입니다.\n" + EDIT)
    assert edits == [("const x = 1;", "const x = 2;")]


def test_parse_multiple_blocks():
    two = EDIT + "\n" + EDIT.replace("x = 1", "y = 1").replace("x = 2", "y = 2")
    edits = parse_edits(two)
    assert len(edits) == 2
    assert edits[1] == ("const y = 1;", "const y = 2;")


def test_parse_empty_replace_allowed():
    block = (
        '<edit path="src/A.tsx">\n<<<<<<< SEARCH\nconst x = 1;\n'
        "=======\n>>>>>>> REPLACE\n</edit>"
    )
    assert parse_edits(block) == [("const x = 1;", "")]


def test_parse_no_edit_tag_raises():
    with pytest.raises(PatchError):
        parse_edits("그냥 설명만 있고 edit 블록 없음")


def test_parse_malformed_missing_separator_raises():
    bad = '<edit path="src/A.tsx">\n<<<<<<< SEARCH\nconst x = 1;\n>>>>>>> REPLACE\n</edit>'
    with pytest.raises(PatchError):
        parse_edits(bad)


def test_parse_incomplete_block_raises():
    bad = '<edit path="src/A.tsx">\n<<<<<<< SEARCH\nconst x = 1;\n======='
    with pytest.raises(PatchError):
        parse_edits(bad)
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_code_patch.py -q`
Expected: FAIL — `ModuleNotFoundError: app.services.code_patch`

- [ ] **Step 3: 최소 구현** — `app/services/code_patch.py`

```python
"""diff(search/replace) 패치 파싱 및 적용 — 순수 함수, 외부 의존 없음.

모델이 <edit> search/replace 블록을 출력하면 parse_edits로 (search, replace)
목록을 만들고, apply_edits로 현재 파일에 순차 적용해 전체 파일을 만든다.
"""
import re

__all__ = ["PatchError", "parse_edits", "apply_edits"]


class PatchError(Exception):
    """패치 파싱/적용 실패. 호출부는 이를 받아 전체출력 재생성으로 폴백한다."""


# <edit ...> <<<<<<< SEARCH \n {search} \n ======= \n {replace} \n >>>>>>> REPLACE </edit>
_EDIT_BLOCK = re.compile(
    r"<edit\b[^>]*>\s*"
    r"<{7}\s*SEARCH\s*\n"
    r"(?P<search>.*?)"
    r"\n?={7}[ \t]*\n"
    r"(?P<replace>.*?)"
    r"\n?>{7}\s*REPLACE\s*"
    r"</edit>",
    re.DOTALL,
)
# 블록처럼 보이지만 마커가 깨진 경우 감지용
_EDIT_OPEN = re.compile(r"<edit\b[^>]*>", re.DOTALL)


def parse_edits(text: str) -> list[tuple[str, str]]:
    """text에서 모든 <edit> 블록을 (search, replace) 목록으로 파싱.

    - 정상 블록 0개이고 <edit 흔적이 있거나, 전혀 없으면 PatchError.
    - <edit 열림 수와 정상 블록 수가 다르면(마커 깨짐) PatchError.
    """
    blocks = [(m.group("search"), m.group("replace")) for m in _EDIT_BLOCK.finditer(text)]
    opens = len(_EDIT_OPEN.findall(text))
    if not blocks:
        raise PatchError("no valid <edit> block found")
    if opens != len(blocks):
        raise PatchError(f"malformed edit block: {opens} <edit> open(s) but {len(blocks)} parsed")
    return blocks


def apply_edits(base: str, edits: list[tuple[str, str]]) -> str:
    """edits를 base에 순차 적용해 전체 파일 문자열 반환.

    각 (search, replace):
      1) 정확 매칭이 유일하면 치환.
      2) 아니면 퍼지 매칭(줄 trailing 공백 무시)이 유일하면 그 줄 범위를 치환.
      3) 둘 다 유일 매칭 실패면 PatchError.
    개행은 \n으로 정규화한 위에서 작업한다.
    """
    if not edits:
        raise PatchError("empty edit list")
    result = base.replace("\r\n", "\n")
    for search, replace in edits:
        search = search.replace("\r\n", "\n")
        replace = replace.replace("\r\n", "\n")
        if search == "":
            raise PatchError("empty SEARCH block")
        if result.count(search) == 1:
            result = result.replace(search, replace, 1)
            continue
        span = _fuzzy_find_unique(result, search)
        if span is None:
            raise PatchError(f"SEARCH not uniquely found: {search[:120]!r}")
        start, end = span
        result = result[:start] + replace + result[end:]
    return result


def _fuzzy_find_unique(text: str, search: str) -> tuple[int, int] | None:
    """줄 단위 비교(각 줄 trailing 공백 제거, 줄 내부 공백 보존)로 search가
    text에서 정확히 1회 매칭되는 char 범위 (start, end)를 반환. 0개/복수면 None.
    text, search는 개행이 \n으로 정규화되어 있다고 가정한다.
    """
    text_lines = text.split("\n")
    search_lines = [ln.rstrip() for ln in search.split("\n")]
    while search_lines and search_lines[-1] == "":
        search_lines.pop()
    if not search_lines:
        return None
    norm = [ln.rstrip() for ln in text_lines]
    n = len(search_lines)
    matches = [i for i in range(len(norm) - n + 1) if norm[i : i + n] == search_lines]
    if len(matches) != 1:
        return None
    i = matches[0]
    start = sum(len(ln) + 1 for ln in text_lines[:i])
    region_len = sum(len(ln) for ln in text_lines[i : i + n]) + (n - 1)
    return (start, start + region_len)
```

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_code_patch.py -q`
Expected: PASS (parse 테스트들)

- [ ] **Step 5: 커밋**

```bash
git add apps/ai-service/app/services/code_patch.py apps/ai-service/tests/test_code_patch.py
git commit -m "Feat: diff 패치 파서 parse_edits + 모듈 골격(code_patch)"
```

### Task 1.2: apply_edits (정확/퍼지/순차/실패)

- [ ] **Step 1: 실패 테스트 추가** — `tests/test_code_patch.py` 하단에 추가

```python
BASE = "const x = 1;\nconst y = 2;\nconsole.log(x, y);\n"


def test_apply_exact_single():
    out = apply_edits(BASE, [("const x = 1;", "const x = 9;")])
    assert "const x = 9;" in out and "const y = 2;" in out


def test_apply_fuzzy_trailing_whitespace():
    # SEARCH 줄 끝 공백이 원본과 달라도 매칭되어야 함
    out = apply_edits(BASE, [("const x = 1;   ", "const x = 9;")])
    assert "const x = 9;" in out


def test_apply_sequential_two_edits():
    out = apply_edits(BASE, [("const x = 1;", "const x = 9;"), ("const y = 2;", "const y = 8;")])
    assert "const x = 9;" in out and "const y = 8;" in out


def test_apply_search_not_found_raises():
    with pytest.raises(PatchError):
        apply_edits(BASE, [("const z = 3;", "const z = 4;")])


def test_apply_ambiguous_search_raises():
    base = "a();\na();\n"
    with pytest.raises(PatchError):
        apply_edits(base, [("a();", "b();")])


def test_apply_later_edit_targets_removed_region_raises():
    # 앞 블록이 지운 영역을 뒤 블록이 노리면 실패해야 함
    edits = [("const x = 1;", "const x = 9;"), ("const x = 1;", "const x = 7;")]
    with pytest.raises(PatchError):
        apply_edits(BASE, edits)


def test_apply_indentation_preserved_internal_whitespace():
    base = "function f() {\n  return 1;\n}\n"
    out = apply_edits(base, [("  return 1;", "  return 2;")])
    assert "  return 2;" in out  # 내부 들여쓰기 보존
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_code_patch.py -q`
Expected: 대부분 PASS이나, 구현 누락분이 있으면 FAIL. (apply_edits는 1.1에서 이미 구현됨 → 통과해야 정상. FAIL 시 구현 수정)

- [ ] **Step 3: 구현 보정** — 테스트가 FAIL이면 `apply_edits`/`_fuzzy_find_unique`를 수정해 통과시킨다. (예상: 1.1 구현으로 전부 통과)

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_code_patch.py -q`
Expected: PASS (전체)

- [ ] **Step 5: 커밋**

```bash
git add apps/ai-service/tests/test_code_patch.py apps/ai-service/app/services/code_patch.py
git commit -m "Test: apply_edits 정확/퍼지/순차/실패 케이스"
```

---

## Chunk 2: 설정 + 프롬프트 분기 + base_code 배선

diff 판정에 필요한 설정, diff용 프롬프트(시스템+사용자), base_code 조회 추출 및 `build_conversation_history` 분기.

**Files:**
- Modify: `apps/ai-service/app/core/config.py` (gemini 설정 옆, ~line 44)
- Modify: `apps/ai-service/app/api/components.py` (`DIFF_RESPONSE_FORMAT_INSTRUCTIONS` 신규 + `generate_system_prompt` diff_mode)
- Modify: `apps/ai-service/app/api/chat.py` (`resolve_system_prompt` diff_mode, `resolve_base_code` 추출, `build_conversation_history` diff 분기)
- Test: `apps/ai-service/tests/test_diff_prompt.py`

### Task 2.1: config 설정 2개 추가

- [ ] **Step 1: 설정 추가** — `app/core/config.py`, `gemini_nocode_max_retries` 정의 바로 아래

```python
    # diff(search/replace) 부분 편집 — 대형 파일 수정 시 변경분만 출력
    gemini_diff_edit_enabled: bool = False  # 마스터 스위치(무중단 롤아웃, 기본 off)
    gemini_diff_edit_threshold_chars: int = 24000  # base 코드 len(content) 초과 시 diff 적용(≈8k 토큰)
```

- [ ] **Step 2: 로딩 확인**

Run: `uv run python -c "from app.core.config import get_settings as g; s=g(); print(s.gemini_diff_edit_enabled, s.gemini_diff_edit_threshold_chars)"`
Expected: `False 24000`

- [ ] **Step 3: 커밋**

```bash
git add apps/ai-service/app/core/config.py
git commit -m "Feat: diff 편집 설정(enabled/threshold) 추가"
```

### Task 2.2: DIFF_RESPONSE_FORMAT_INSTRUCTIONS + generate_system_prompt diff_mode

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_diff_prompt.py`

```python
"""diff 모드 프롬프트 분기 테스트."""
from app.api.components import (
    DIFF_RESPONSE_FORMAT_INSTRUCTIONS,
    RESPONSE_FORMAT_INSTRUCTIONS,
    generate_system_prompt,
)


def _schema():
    return {"components": []}


def test_diff_format_instructions_mention_edit_blocks():
    assert "<edit" in DIFF_RESPONSE_FORMAT_INSTRUCTIONS
    assert "SEARCH" in DIFF_RESPONSE_FORMAT_INSTRUCTIONS
    assert "REPLACE" in DIFF_RESPONSE_FORMAT_INSTRUCTIONS


def test_generate_system_prompt_full_uses_file_format():
    p = generate_system_prompt(_schema())
    assert RESPONSE_FORMAT_INSTRUCTIONS.strip()[:40] in p
    assert DIFF_RESPONSE_FORMAT_INSTRUCTIONS not in p


def test_generate_system_prompt_diff_uses_edit_format():
    p = generate_system_prompt(_schema(), diff_mode=True)
    assert DIFF_RESPONSE_FORMAT_INSTRUCTIONS in p
    # 전체출력(<file>) 포맷 지시는 들어가지 않아야 충돌 없음
    assert RESPONSE_FORMAT_INSTRUCTIONS not in p
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_diff_prompt.py -q`
Expected: FAIL — `ImportError: cannot import name 'DIFF_RESPONSE_FORMAT_INSTRUCTIONS'`

- [ ] **Step 3: 구현** — `app/api/components.py`, `RESPONSE_FORMAT_INSTRUCTIONS` 정의(2066) 바로 아래에 신규 상수 추가

```python
DIFF_RESPONSE_FORMAT_INSTRUCTIONS = """

## FORMAT (부분 수정 모드)
1. 간단한 한글 설명 (1-2문장)
2. **변경된 부분만** 아래 SEARCH/REPLACE 형식으로 출력. `<file>` 전체 출력 절대 금지.

```
<edit path="src/...">
<<<<<<< SEARCH
{현재 코드에서 공백·들여쓰기까지 그대로 복사한 스니펫 — 파일에서 유일하게 식별되도록 충분한 컨텍스트 포함}
=======
{바뀐 스니펫}
>>>>>>> REPLACE
</edit>
```

### 규칙 (절대 준수)
- SEARCH 블록은 현재 코드와 **글자 그대로** 일치해야 한다(들여쓰기 포함).
- 변경이 없는 부분은 출력하지 말 것. 여러 곳을 고치면 `<edit>` 블록을 여러 개 낸다.
- SEARCH는 파일에서 한 번만 매칭되도록 충분한 주변 줄을 포함한다.
- 전체 파일이나 `<file>` 태그를 출력하지 말 것.
"""
```

- [ ] **Step 4: generate_system_prompt에 diff_mode 추가** — 시그니처(2406-2414)에 파라미터 추가, 조립부(2465) 교체

시그니처 끝에 추가:
```python
    component_usage_map: dict | None = None,
    diff_mode: bool = False,
) -> str:
```
조립부 `+ RESPONSE_FORMAT_INSTRUCTIONS`(2465) 를 다음으로 교체:
```python
        + (DIFF_RESPONSE_FORMAT_INSTRUCTIONS if diff_mode else RESPONSE_FORMAT_INSTRUCTIONS)
```
> ⚠️ `+ RESPONSE_FORMAT_INSTRUCTIONS` 는 파일에 3곳(2117 모듈 `SYSTEM_PROMPT` 상수, 2465 `generate_system_prompt`, 2572 `get_vision_system_prompt`) 있다. **2465만** 교체해야 한다 — Edit 시 앞줄 `+ (UI_PATTERN_EXAMPLES if not skip_ui_patterns else "")` 를 포함한 고유 컨텍스트로 매칭(`replace_all=false`). vision(2572)·모듈상수(2117)는 건드리지 않는다(diff 비대상).

- [ ] **Step 5: 통과 확인**

Run: `uv run pytest tests/test_diff_prompt.py -q`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add apps/ai-service/app/api/components.py apps/ai-service/tests/test_diff_prompt.py
git commit -m "Feat: diff 응답 포맷 프롬프트 + generate_system_prompt diff_mode"
```

### Task 2.3: resolve_system_prompt diff_mode 패스스루

- [ ] **Step 1: 구현** — `app/api/chat.py` `resolve_system_prompt`(244)

시그니처 keyword-only 영역에 추가:
```python
    *,
    skip_ui_patterns: bool = False,
    diff_mode: bool = False,
) -> str:
```
`generate_system_prompt(...)` 호출(292-296)에 `diff_mode=diff_mode` 전달:
```python
    base_prompt = generate_system_prompt(
        schema, design_tokens, ag_grid_schema, ag_grid_tokens, component_definitions,
        skip_ui_patterns=skip_ui_patterns,
        component_usage_map=component_usage_map,
        diff_mode=diff_mode,
    )
```

- [ ] **Step 2: 임포트/스모크 확인**

Run: `uv run python -c "import app.api.chat"`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add apps/ai-service/app/api/chat.py
git commit -m "Feat: resolve_system_prompt diff_mode 패스스루"
```

### Task 2.4: resolve_base_code 추출 + build_conversation_history diff 분기

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_diff_prompt.py` 하단 추가

```python
import app.api.chat as chat_module
from app.schemas.chat import Message  # 실제 위치에 맞게 (Message 임포트 경로 확인)


async def test_build_history_diff_uses_edit_instruction(monkeypatch):
    base = {"path": "src/A.tsx", "content": "const x = 1;"}
    msgs = await chat_module.build_conversation_history(
        room_id="r1", system_prompt="SYS", current_message="x를 2로",
        base_code=base, diff_mode=True,
    )
    user = msgs[-1].content
    assert "현재 코드:" in user and 'src/A.tsx' in user  # SEARCH 출처로 현재 코드 주입 유지
    assert "<edit" in user or "SEARCH" in user  # diff 지시 포함
    assert "전체 코드를 빠짐없이 출력" not in user  # 전체출력 지시는 없어야


async def test_build_history_full_uses_file_instruction(monkeypatch):
    base = {"path": "src/A.tsx", "content": "const x = 1;"}
    msgs = await chat_module.build_conversation_history(
        room_id="r1", system_prompt="SYS", current_message="x를 2로",
        base_code=base, diff_mode=False,
    )
    user = msgs[-1].content
    assert "전체 코드를 빠짐없이 출력" in user
```

> `Message` 임포트 경로는 chat.py 상단의 실제 임포트를 따른다(`from app.schemas.chat import Message` 형태). 테스트가 ImportError면 경로만 맞춘다.

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_diff_prompt.py -q -k build_history`
Expected: FAIL — `build_conversation_history() got an unexpected keyword argument 'base_code'`

- [ ] **Step 3: resolve_base_code 추출** — `app/api/chat.py`, `build_conversation_history`(309) **위에** 신규 함수 추가

```python
_UNSET = object()


async def resolve_base_code(room_id: str, from_message_id: str | None = None) -> dict | None:
    """수정의 기준 코드 1건 조회: from_message_id 우선, 없으면 방의 최신 코드 메시지.

    Returns: {"path": str, "content": str} 또는 None(첫 생성).
    """
    if from_message_id:
        base_message = await get_message_by_id(from_message_id)
        if base_message is None:
            raise ValueError(f"Message not found: {from_message_id}")
        if base_message.get("content"):
            base = {
                "path": base_message.get("path", "src/Component.tsx"),
                "content": base_message["content"],
            }
            logger.info(
                "Code context from specified message",
                extra={"from_message_id": from_message_id, "path": base["path"]},
            )
            return base
        return None
    latest_code_msg = await get_latest_code_message(room_id)
    if latest_code_msg and latest_code_msg.get("content") and latest_code_msg.get("path"):
        base = {"path": latest_code_msg["path"], "content": latest_code_msg["content"]}
        logger.info(
            "Code context from latest message",
            extra={"room_id": room_id, "path": base["path"]},
        )
        return base
    return None
```

- [ ] **Step 4: build_conversation_history 분기** — 시그니처(309-314)를 교체

```python
async def build_conversation_history(
    room_id: str,
    system_prompt: str,
    current_message: str,
    from_message_id: str | None = None,
    *,
    base_code: dict | None = _UNSET,  # type: ignore[assignment]
    diff_mode: bool = False,
) -> list[Message]:
```
본문에서 base_code 조회 블록(334-362)을 다음으로 교체:
```python
    messages = [Message(role="system", content=system_prompt)]

    # 기준 코드: 호출부가 미리 넘긴 값 우선, 아니면 직접 조회(하위호환)
    if base_code is _UNSET:
        base_code = await resolve_base_code(room_id, from_message_id)
```
이어지는 `if base_code:` 사용자 메시지 구성(365-377)을 diff/full 분기로 교체:
```python
    if base_code:
        code_context = f'현재 코드:\n<file path="{base_code["path"]}">{base_code["content"]}</file>'
        if diff_mode:
            final_message = f'''{code_context}

요청: {current_message}

⚠️ 부분 수정 모드 (절대 준수):
- 변경이 필요한 부분만 SEARCH/REPLACE(<edit>) 형식으로 출력할 것
- SEARCH 블록은 위 현재 코드와 글자 그대로(들여쓰기 포함) 일치해야 함
- <file> 전체 출력 금지. 요청과 무관한 부분은 건드리지 말 것
- 여러 곳을 고치면 <edit> 블록을 여러 개 낼 것'''
        else:
            final_message = f'''{code_context}

요청: {current_message}

⚠️ 수정 규칙 (절대 준수):
- 위 요청에 해당하는 부분만 정확히 수정할 것
- 요청과 무관한 텍스트, 색상, 레이아웃, 스타일, 컴포넌트 구조는 절대 변경하지 말 것
- 변수명, 함수명, import, 더미 데이터도 기존 그대로 유지할 것
- 기존에 없던 UI 요소(타이틀, 필터, 안내문 등)를 임의로 추가하지 말 것
- 전체 코드를 빠짐없이 출력할 것 (생략 시 빈 화면 발생)
- 수정 전후 diff가 요청 범위 안에서만 발생해야 함'''
    else:
```
(이하 `else:` 첫 메시지 블록 378-390은 그대로 유지)

- [ ] **Step 5: 통과 확인**

Run: `uv run pytest tests/test_diff_prompt.py -q`
Expected: PASS

- [ ] **Step 6: 기존 호출부 회귀 확인** — chat.py:826(non-stream)은 `base_code`/`diff_mode` 미전달 → `_UNSET` 기본값으로 내부 조회(기존 동작 유지). 변경 불필요.

Run: `uv run pytest tests/test_chat.py -q`
Expected: PASS (기존 chat 동작 회귀 없음)

- [ ] **Step 7: 커밋**

```bash
git add apps/ai-service/app/api/chat.py apps/ai-service/tests/test_diff_prompt.py
git commit -m "Refactor: resolve_base_code 추출 + build_conversation_history diff 분기"
```

---

## Chunk 3: StreamingParser diff 모드

패치 텍스트를 broadcast하지 않고 누적하는 파서 모드. 설명 텍스트(첫 `<edit>` 이전)는 라이브 chat으로 emit.

**Files:**
- Modify: `apps/ai-service/app/api/chat.py` (`StreamingParser` 637-717)
- Test: `apps/ai-service/tests/test_diff_parser.py`

### Task 3.1: StreamingParser(mode="diff")

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_diff_parser.py`

```python
"""StreamingParser diff 모드: 설명은 chat emit, 패치는 누적(broadcast 안 함)."""
from app.api.chat import StreamingParser

PATCH = (
    '<edit path="src/A.tsx">\n<<<<<<< SEARCH\nconst x = 1;\n'
    "=======\nconst x = 2;\n>>>>>>> REPLACE\n</edit>"
)


def _feed(parser, full, n=7):
    """문자열을 n자씩 청크로 흘려보내며 이벤트 수집."""
    events = []
    for i in range(0, len(full), n):
        events += parser.process_chunk(full[i : i + n])
    events += parser.flush()
    return events


def test_diff_chat_before_edit_is_emitted():
    p = StreamingParser(mode="diff")
    events = _feed(p, "x를 2로 바꿨습니다.\n" + PATCH)
    chat_text = "".join(e["text"] for e in events if e["type"] == "chat")
    assert "바꿨습니다" in chat_text


def test_diff_patch_not_emitted_as_chat():
    p = StreamingParser(mode="diff")
    events = _feed(p, "설명\n" + PATCH)
    all_text = "".join(e.get("text", "") for e in events)
    assert "<<<<<<< SEARCH" not in all_text  # 패치는 broadcast되지 않음
    assert not any(e["type"] == "code" for e in events)


def test_diff_get_patch_returns_full_patch():
    p = StreamingParser(mode="diff")
    _feed(p, "설명\n" + PATCH)
    patch = p.get_patch()
    assert "<<<<<<< SEARCH" in patch and ">>>>>>> REPLACE" in patch
    assert patch.count("<edit") == 1


def test_file_mode_unchanged():
    p = StreamingParser()  # 기본 file 모드
    events = _feed(p, '설명 <file path="src/A.tsx">const a=1;</file>')
    assert any(e["type"] == "code" for e in events)
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_diff_parser.py -q`
Expected: FAIL — `StreamingParser() takes no arguments` / `no attribute 'get_patch'`

- [ ] **Step 3: 구현** — `app/api/chat.py` `StreamingParser`

클래스 패턴 상수 옆에 추가:
```python
    EDIT_START_PATTERN = re.compile(r"<edit\b")
```
`__init__`(644) 교체:
```python
    def __init__(self, mode: str = "file"):
        self.mode = mode
        self.buffer = ""
        self.inside_file = False
        self.current_file_path: str | None = None
        self.current_file_content = ""
        # diff 모드 전용
        self.in_patch = False
        self.patch_buffer = ""
```
`process_chunk`(650) 본문 맨 앞에 diff 분기 추가:
```python
    def process_chunk(self, chunk: str) -> list[dict]:
        if self.mode == "diff":
            return self._process_diff_chunk(chunk)
        # --- 이하 기존 file 모드 로직 그대로 ---
        self.buffer += chunk
        ...
```
클래스에 신규 메서드 추가(process_chunk 아래):
```python
    def _process_diff_chunk(self, chunk: str) -> list[dict]:
        """diff 모드: 첫 <edit 이전 텍스트만 chat으로 emit, 이후는 patch_buffer에 누적."""
        self.buffer += chunk
        events: list[dict] = []
        if self.in_patch:
            self.patch_buffer += self.buffer
            self.buffer = ""
            return events
        m = self.EDIT_START_PATTERN.search(self.buffer)
        if m:
            before = self.buffer[: m.start()]
            if before:
                events.append({"type": "chat", "text": before})
            self.patch_buffer += self.buffer[m.start() :]
            self.buffer = ""
            self.in_patch = True
            return events
        # 아직 <edit 없음: '<' 직전까지만 chat으로 보내고 부분 태그는 홀드백
        tag_start = self.buffer.rfind("<")
        if tag_start > 0:
            events.append({"type": "chat", "text": self.buffer[:tag_start]})
            self.buffer = self.buffer[tag_start:]
        elif tag_start == -1 and self.buffer:
            events.append({"type": "chat", "text": self.buffer})
            self.buffer = ""
        return events

    def get_patch(self) -> str:
        """diff 모드: 누적된 패치 텍스트 반환(스트림 종료 후 호출)."""
        return self.patch_buffer + (self.buffer if self.in_patch else "")
```
`flush`(711)에 diff 모드 처리 추가(메서드 맨 앞):
```python
    def flush(self) -> list[dict]:
        if self.mode == "diff":
            events: list[dict] = []
            if not self.in_patch and self.buffer.strip():
                events.append({"type": "chat", "text": self.buffer.strip()})
            return events
        # --- 이하 기존 file 모드 flush 그대로 ---
        events: list[dict] = []
        remaining = self.buffer.strip()
        ...
```

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_diff_parser.py -q`
Expected: PASS (file_mode_unchanged 포함)

- [ ] **Step 5: 파서 회귀 확인**

Run: `uv run pytest tests/test_chat.py tests/test_chunk_coalesce.py -q`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add apps/ai-service/app/api/chat.py apps/ai-service/tests/test_diff_parser.py
git commit -m "Feat: StreamingParser diff 모드(패치 누적, broadcast 안 함)"
```

---

## Chunk 4: _run_broadcast_generation diff 분기 + 엔드포인트 배선

diff 사전 단계(단일 생성 → 패치 적용)와 PatchError 시 전체출력 폴백, 엔드포인트에서 diff 판정·배선.

**Files:**
- Modify: `apps/ai-service/app/api/chat.py` (`_run_broadcast_generation` 1117-, chat_stream 엔드포인트 1022-1052)
- Test: `apps/ai-service/tests/test_diff_generation.py`

### Task 4.1: _run_broadcast_generation diff 분기

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_diff_generation.py`

```python
"""diff 모드 생성 통합: 패치 적용→전체파일 저장, 실패→전체출력 폴백, 패치 비broadcast."""
from app.api import chat as chat_module

BASE = {"path": "src/A.tsx", "content": "const x = 1;\nexport default 0;\n"}
PATCH_TEXT = (
    "x를 2로 바꿉니다.\n"
    '<edit path="src/A.tsx">\n<<<<<<< SEARCH\nconst x = 1;\n'
    "=======\nconst x = 2;\n>>>>>>> REPLACE\n</edit>"
)
FILE_CODE = '<file path="src/A.tsx">const x = 2;\nexport default 0;</file>'


class _FakeProvider:
    def __init__(self, scripts):
        self._scripts = scripts
        self.calls = 0

    async def chat_stream(self, messages):
        script = self._scripts[self.calls]
        self.calls += 1
        for piece in script:
            yield piece


def _patch(monkeypatch, saves, events):
    async def fake_save(**kw):
        saves.append(kw)

    async def fake_broadcast(room_id, event, payload, **kw):
        events.append((event, payload))

    async def fake_update(**kw):
        pass

    monkeypatch.setattr(chat_module, "_save_message_with_retry", fake_save)
    monkeypatch.setattr(chat_module, "broadcast_event", fake_broadcast)
    monkeypatch.setattr(chat_module, "update_chat_message", fake_update)


async def test_diff_success_applies_patch_and_saves_full_file(monkeypatch):
    provider = _FakeProvider([[PATCH_TEXT]])
    saves, events = [], []
    _patch(monkeypatch, saves, events)

    await chat_module._run_broadcast_generation(
        room_id="r1", message_id="m1", user_id="u1",
        provider=provider, messages=[], images=[], is_vision_mode=False,
        is_diff_mode=True, base_code=BASE, fallback_messages=[],
    )

    assert provider.calls == 1, "diff는 단일 생성"
    done = [s for s in saves if s.get("status") == "DONE"]
    assert done, f"DONE 저장 없음: {saves}"
    assert done[-1]["content"] == "const x = 2;\nexport default 0;\n"  # 패치 적용된 전체 파일
    # 패치 원문이 chat으로 broadcast되지 않음
    chat_text = "".join(p.get("text", "") for e, p in events if e == "chunk")
    assert "<<<<<<< SEARCH" not in chat_text


async def test_diff_patch_error_falls_back_to_full_output(monkeypatch):
    # diff 생성은 매칭 불가 패치(폴백 유발) → 전체출력 폴백에서 <file> 생성
    bad_patch = (
        '<edit path="src/A.tsx">\n<<<<<<< SEARCH\nNONEXISTENT LINE\n'
        "=======\nwhatever\n>>>>>>> REPLACE\n</edit>"
    )
    provider = _FakeProvider([[bad_patch], [FILE_CODE]])
    saves, events = [], []
    _patch(monkeypatch, saves, events)

    await chat_module._run_broadcast_generation(
        room_id="r1", message_id="m1", user_id="u1",
        provider=provider, messages=[], images=[], is_vision_mode=False,
        is_diff_mode=True, base_code=BASE, fallback_messages=[],
    )

    assert provider.calls == 2, "PatchError → 전체출력 재생성 1회"
    done = [s for s in saves if s.get("status") == "DONE"]
    assert done and "const x = 2;" in done[-1]["content"]


async def test_non_diff_path_unchanged(monkeypatch):
    provider = _FakeProvider([[FILE_CODE]])
    saves, events = [], []
    _patch(monkeypatch, saves, events)
    await chat_module._run_broadcast_generation(
        room_id="r1", message_id="m1", user_id="u1",
        provider=provider, messages=[], images=[], is_vision_mode=False,
    )
    assert provider.calls == 1
    assert any(s.get("status") == "DONE" for s in saves)
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_diff_generation.py -q`
Expected: FAIL — `unexpected keyword argument 'is_diff_mode'`

- [ ] **Step 3: 구현 — 시그니처/임포트** — `app/api/chat.py`

상단 임포트에 추가:
```python
from app.services.code_patch import PatchError, apply_edits, parse_edits
```
`_run_broadcast_generation` 시그니처(1117-1129) 끝에 추가:
```python
    user_message: str | None = None,
    is_diff_mode: bool = False,
    base_code: dict | None = None,
    fallback_messages: list | None = None,
) -> None:
```

- [ ] **Step 4: 구현 — diff 사전 단계** — `try:` 블록 안, `chunk_sender_task = asyncio.create_task(_chunk_sender())`(1215) **다음**, `max_attempts = ...`(1221) **앞**에 삽입

```python
        # diff 사전 단계: 패치만 생성 → 적용해 전체 파일 확보. 실패 시 전체출력으로 폴백.
        # 주의: PatchError(파싱/적용 실패)만 폴백한다. 스트림 타임아웃/예외는 의도적으로
        # 잡지 않고 바깥 try의 TimeoutError/Exception 핸들러로 전파(표준 루프와 동일 처리).
        if is_diff_mode and base_code:
            diff_parser = StreamingParser(mode="diff")
            try:
                async with asyncio.timeout(600):
                    async for chunk in provider.chat_stream(messages):
                        for event in diff_parser.process_chunk(chunk):
                            etype = event.pop("type")
                            if etype == "chat":
                                collected_text += event.get("text", "")
                            await chunk_queue.put({"type": etype, **event})
                for event in diff_parser.flush():
                    etype = event.pop("type")
                    if etype == "chat":
                        collected_text += event.get("text", "")
                    await chunk_queue.put({"type": etype, **event})

                edits = parse_edits(diff_parser.get_patch())
                full_content = apply_edits(base_code["content"], edits)
                collected_files = [{"path": base_code["path"], "content": full_content}]
                logger.info(
                    "Diff edit applied",
                    extra={
                        "room_id": room_id, "message_id": message_id,
                        "edits": len(edits), "result_len": len(full_content),
                    },
                )
            except PatchError as e:
                logger.warning(
                    "Diff patch failed — falling back to full output",
                    extra={"room_id": room_id, "message_id": message_id, "error": str(e)},
                )
                # 폴백: 전체출력 모드로 재생성 (아래 표준 루프가 처리)
                is_diff_mode = False
                if fallback_messages is not None:
                    messages = fallback_messages
                parser = StreamingParser()
                collected_text = ""
                collected_files = []
                await chunk_queue.put({"type": "retry", "attempt": 1})
```

- [ ] **Step 5: 구현 — 표준 루프를 조건부로** — 기존 `max_attempts = get_settings()...`(1221)부터 `for attempt ...` 루프 전체(1221-1307)를 `if not collected_files:` 블록으로 감싼다

```python
        # diff 성공 시 collected_files가 이미 채워져 루프를 건너뛴다.
        if not collected_files:
            max_attempts = get_settings().gemini_nocode_max_retries + 1
            for attempt in range(max_attempts):
                ...  # 기존 루프 본문 그대로 (들여쓰기 한 단계 추가)
                if collected_files:
                    break
```
(루프 이후의 drain/가드/DONE 저장/done broadcast 1309-1365는 그대로 — diff 성공/폴백 모두 동일 경로로 합류)

- [ ] **Step 6: 통과 확인**

Run: `uv run pytest tests/test_diff_generation.py -q`
Expected: PASS (3 테스트)

- [ ] **Step 7: 생성 경로 회귀 확인**

Run: `uv run pytest tests/test_nocode_retry.py tests/test_derail_guard.py tests/test_connection_guard.py -q`
Expected: PASS

- [ ] **Step 8: 커밋**

```bash
git add apps/ai-service/app/api/chat.py apps/ai-service/tests/test_diff_generation.py
git commit -m "Feat: _run_broadcast_generation diff 분기 + PatchError 전체출력 폴백"
```

### Task 4.2: chat_stream 엔드포인트 diff 판정·배선

- [ ] **Step 1: 구현 — diff 판정** — `app/api/chat.py` chat_stream, 시스템 프롬프트 생성 분기(1001-1027) **앞**에 base_code/diff 판정 추가

```python
        # diff(부분 편집) 판정: 스위치 ON + 대형 수정 + figma/vision 아님
        _settings = get_settings()
        base_code = await resolve_base_code(request.room_id, request.from_message_id)
        is_diff_mode = (
            _settings.gemini_diff_edit_enabled
            and base_code is not None
            and len(base_code["content"]) > _settings.gemini_diff_edit_threshold_chars
            and not is_figma_mode
            and not is_vision_mode
        )
```

- [ ] **Step 2: 구현 — 일반 모드 시스템 프롬프트에 diff_mode 전달** — `else:` 일반 분기(1022-1027)의 `resolve_system_prompt(...)`에 `diff_mode=is_diff_mode` 추가

```python
        else:
            system_prompt = await resolve_system_prompt(
                schema_key=room.get("schema_key"),
                current_composition=request.current_composition,
                selected_instance_id=request.selected_instance_id,
                diff_mode=is_diff_mode,
            )
```

- [ ] **Step 3: 구현 — messages + fallback_messages 빌드** — `messages = await build_conversation_history(...)`(1030-1035) 교체

```python
        # 5. 메시지 빌드 (diff면 base_code 재사용 + diff 지시)
        messages = await build_conversation_history(
            room_id=request.room_id,
            system_prompt=system_prompt,
            current_message=request.message,
            from_message_id=request.from_message_id,
            base_code=base_code,
            diff_mode=is_diff_mode,
        )
        # diff 실패 시 폴백용 전체출력 메시지(전체 포맷 시스템 프롬프트 + 전체출력 지시)
        fallback_messages = None
        if is_diff_mode:
            full_system_prompt = await resolve_system_prompt(
                schema_key=room.get("schema_key"),
                current_composition=request.current_composition,
                selected_instance_id=request.selected_instance_id,
                diff_mode=False,
            )
            fallback_messages = await build_conversation_history(
                room_id=request.room_id,
                system_prompt=full_system_prompt,
                current_message=request.message,
                from_message_id=request.from_message_id,
                base_code=base_code,
                diff_mode=False,
            )
```

- [ ] **Step 4: 구현 — _run_broadcast_generation 호출에 인자 추가** — task 생성부(1038-1050)의 `_run_broadcast_generation(...)` 인자 끝에 추가

```python
                system_prompt=system_prompt if is_figma_mode else None,
                user_message=request.message if is_figma_mode else None,
                is_diff_mode=is_diff_mode,
                base_code=base_code,
                fallback_messages=fallback_messages,
```

- [ ] **Step 5: 임포트/스모크 확인**

Run: `uv run python -c "import app.api.chat"`
Expected: 에러 없음

- [ ] **Step 6: 전체 테스트**

Run: `uv run pytest -q`
Expected: PASS (전체 스위트)

- [ ] **Step 7: 린트/타입 확인**

Run: `uv run ruff check app/api/chat.py app/api/components.py app/services/code_patch.py app/core/config.py`
Expected: 통과 (위반 시 수정)

- [ ] **Step 8: 커밋**

```bash
git add apps/ai-service/app/api/chat.py
git commit -m "Feat: chat_stream diff 판정·배선(base_code/diff_mode/fallback)"
```

---

## Chunk 5: 수동 검증 & 롤아웃 (코드 변경 없음)

스위치 off 기본이므로 배포해도 무동작. dev에서 스위치 ON 후 복구방(37af968f, ~115KB)으로 실측한다.

### Task 5.1: dev 검증

- [ ] **Step 1: dev에 `GEMINI_DIFF_EDIT_ENABLED=true` 설정 후 배포** — `deploy-ai-service.yml`의 dev env 또는 Cloud Run 환경변수에 추가. (배포는 사용자 승인 후)

- [ ] **Step 2: 복구방 편집 1~2회 실행** — 기존 `scratchpad/edit_test.py` 패턴으로 작은 수정 요청 → DB에서 `status=DONE` + `content`가 ~115KB 전체 파일인지 확인.

- [ ] **Step 3: 로그 확인** — Cloud Run 로그에서 `Diff edit applied`(edits/result_len) 또는 `Diff patch failed`(폴백) 빈도 확인. 폴백률이 높으면 프롬프트 예시 강화 검토. **유력 원인 후보: diff 모드에서도 항상 붙는 `FINAL_REMINDER`("코드 생략 없는가" 등 전체출력용 문구)가 `<edit>` 포맷과 충돌해 모델이 전체 파일을 내게 유도** → 폴백률 높으면 `generate_system_prompt`에서 diff 모드 시 `FINAL_REMINDER`를 트림/가드하는 것을 1순위로 검토(코드품질 리뷰 Chunk1/2 지적).

- [ ] **Step 4: 출력 토큰 비교** — 동일 수정의 diff vs 전체출력 candidates_token_count 비교(변경분 비례로 감소 확인).

### Task 5.2: prod 롤아웃 (별도 승인)

- [ ] **Step 1: dev 안정 확인 후** prod env에 `GEMINI_DIFF_EDIT_ENABLED=true` 설정 → main 배포. (사용자 명시 승인 시에만)

- [ ] **Step 2: prod 폴백률·실패율 모니터링** — 이상 시 스위치 off로 즉시 무중단 롤백.

---

## 검증 체크리스트 (완료 기준)

- [ ] `uv run pytest -q` 전체 통과 (신규: test_code_patch / test_diff_prompt / test_diff_parser / test_diff_generation)
- [ ] 스위치 off일 때 기존 동작 100% 동일 (회귀 테스트 통과)
- [ ] diff 성공 시 DB에 전체 파일 저장(FE 무변경)
- [ ] PatchError 시 전체출력 폴백으로 자동 복구
- [ ] 패치 원문(`<<<<<<< SEARCH`)이 FE로 broadcast되지 않음
- [ ] `ruff check` 통과
