/** @type {import('tailwindcss').Config} */
// 자동 생성된 파일이 아닙니다. 필요에 따라 수정하세요.

module.exports = {
  presets: [require('./tailwind.preset.js')],
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  plugins: [
    // tailwind-scrollbar 플러그인 제거 (커스텀 CSS 사용)
  ],
};
